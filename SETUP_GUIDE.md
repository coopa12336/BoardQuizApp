# セットアップ完全ガイド（初めての方向け・手順を省略せず記載）

このガイドは、プログラミングやサーバーの知識が無い方でも迷わないよう、
「どの画面で」「どこをクリックして」「何を入力するか」まで省略せずに書いています。
上から順番に進めれば、公開URLでアプリが使えるようになります。

所要時間の目安: 30分〜1時間（Arduinoの配線を除く）

---

## 全体の流れ（まずは地図）

1. GitHubアカウントを作る（ソースコードの置き場所）
2. もらったZIPファイルをGitHubにアップロードする
3. Render.comアカウントを作る（サーバーを動かす場所）
4. RenderとGitHubを連携してデプロイする
5. 発行されたURLで動作確認する
6. （使う場合）Arduinoを配線してスケッチを書き込む
7. 当日、投影PCのブラウザからArduinoに接続する

git（バージョン管理ソフト）のインストールやコマンド入力は一切不要です。
すべてブラウザ上の操作だけで完結します。

---

## 1. GitHubアカウントを作る

GitHubは、ソースコード（プログラムのファイル一式）を置いておく無料のサービスです。
Renderがこの置き場所からコードを読み取ってサーバーを動かします。

1. ブラウザで https://github.com/signup を開く
2. メールアドレス、パスワード、ユーザー名を入力して画面の指示に従って進める
   （メール認証コードの入力を求められます）
3. 「What type of user are you?」のような質問が出たら、個人利用として
   スキップ・適当な選択で問題ありません（Renderとの連携以外では使わないため）
4. GitHubのトップページ（例: `https://github.com/あなたのユーザー名`）が
   表示されればアカウント作成完了です

## 2. コードをGitHubにアップロードする

git コマンドを使わず、ブラウザ上のドラッグ＆ドロップだけでアップロードできます。

### 2.1 まず、もらったZIPファイルを解凍しておく

- お使いのPCで `quiz-board-app.zip` をダブルクリック（Windows）または
  ダブルクリック→「開く」（Mac）で解凍してください
- `quiz-board-app` というフォルダができ、その中に `server.js` や
  `public` フォルダなどが入っていることを確認してください

### 2.2 新しいリポジトリ（保管場所）を作る

1. GitHubにログインした状態で右上の「+」アイコン →「New repository」をクリック
2. 「Repository name」に `quiz-board-app` など好きな名前を入力
   （空白や日本語は避けてください）
3. 「Public」のままでも「Private」でも構いません
   （Privateにするとソースコードが他人から見えなくなります。Renderとの連携には
   影響しません）
4. その他の項目（README追加など）は初期設定のままでOK
5. 「Create repository」をクリック

### 2.3 ファイルをアップロードする

1. 作成されたリポジトリのページで「uploading an existing file」というリンクを
   クリック（見当たらない場合は「Add file」→「Upload files」）
2. 解凍した `quiz-board-app` フォルダの**中身**（`server.js`, `package.json`,
   `public` フォルダ, `arduino` フォルダ, `README.md`, `render.yaml` など）を
   まとめて選択し、アップロード画面へドラッグ＆ドロップする
   - **注意**: `quiz-board-app` フォルダそのものではなく、フォルダの「中身」を
     アップロードしてください。フォルダごとドラッグするとブラウザによっては
     階層がずれてしまう場合があります。うまくいかない場合はフォルダごと
     ドラッグしても大抵は問題なく認識されます（GitHub側で自動的に展開されます）
3. 画面下部の「Commit changes」ボタンをクリック（コメント欄は空欄のままで可）
4. アップロードが終わると、リポジトリのページに `server.js` や `public` などの
   ファイル一覧が表示されます。これでコードの準備は完了です

---

## 3. Render.comアカウントを作る

1. ブラウザで https://render.com を開く
2. 右上の「Get Started」または「Sign Up」をクリック
3. 「Sign up with GitHub」を選ぶと簡単です（先ほど作ったGitHubアカウントで
   そのままログインできます）
4. GitHub側で「Authorize Render」のような確認画面が出たら許可する
5. Renderのダッシュボード画面が表示されればアカウント作成完了です
   （クレジットカード登録は無料プランの利用だけであれば不要です）

## 4. Renderでサーバーを公開する（デプロイ）

1. Renderのダッシュボードで「New +」ボタン（右上あたり）をクリック
2. メニューから「Web Service」を選択
3. 「Build and deploy from a Git repository」を選び「Next」
4. 先ほど作成した `quiz-board-app` リポジトリを一覧から探して「Connect」
   - 一覧に出てこない場合は「Configure account」からRenderに
     リポジトリへのアクセス権限を追加してください
5. 設定画面が表示されるので、以下のとおり入力・確認します

   | 項目 | 入力する内容 |
   |---|---|
   | Name | 好きな名前（例: `quiz-board-app`）。これがURLの一部になります |
   | Region | 「Singapore」など、日本から近いリージョンを選ぶと反応が速くなります |
   | Branch | `main`（そのままでOK） |
   | Build Command | `npm install` |
   | Start Command | `npm start` |
   | Instance Type | 「Free」を選択（まずは無料で試せます） |

6. 一番下の「Deploy Web Service」ボタンをクリック
7. 画面にログが流れ始めます。`npm install` の実行 → `npm start` の実行 →
   「早押し・ボードクイズサーバー起動」のようなログが出れば成功です
   （数分かかることがあります）
8. デプロイが完了すると、画面上部に
   `https://quiz-board-app-xxxx.onrender.com` のようなURLが表示されます。
   これが**参加者全員に共有するURL**です

> `render.yaml` を同梱しているため、上記の代わりに Render の
> 「New +」→「Blueprint」から、リポジトリを選ぶだけでも同様の設定が
> 自動で読み込まれます（お好きな方法で構いません）。

## 5. 動作確認する

1. 発行されたURL（例: `https://quiz-board-app-xxxx.onrender.com`）を
   自分のブラウザで開く → ホーム画面が表示されることを確認
2. 「部屋を作る」でテスト用の部屋を作成
3. 別のブラウザタブ（またはスマホ）で同じURLを開き、「回答者として入る」で
   同じ部屋名に入室し、ボードに何か書いてみる
4. 判定者画面・投影画面でも正しく表示されることを確認

これで公開URLでの運用準備は完了です。大会当日はこのURLを判定者・回答者・
投影PC全員に共有するだけで参加できます。

> **無料プランの注意**: しばらくアクセスが無いとサーバーが自動的にスリープし、
> 次のアクセス時に起動し直すまで数十秒〜1分ほどかかります。**大会本番の
> 15分前までにはURLを一度開いてサーバーを起こしておいてください。**
> また、スリープ復帰や再デプロイが起きると、それまでの部屋データは消えます。

---

## 6. 早押し機の準備（使う場合のみ・任意）

早押し機能を使わない場合は、この章は飛ばして構いません。ボード解答だけで
アプリは通常どおり使えます。

このアプリは2種類の早押し機に対応しています。お使いの機材に合わせて
6-A または 6-B のどちらかに進んでください（どちらもサイト側の設定は不要です）。

### 6-A. RP2040マイコンボードキット早押し機（10〜12端子早押し機）をお持ちの場合

すでに完成している市販キットのため、**改造や書き換えは不要**です。

1. 早押し機とパソコン（投影PC）をUSBケーブルで接続する
2. 投影PCでパソコン版のChromeまたはEdgeを開き、`display.html`（プロジェクター
   表示画面）を開く
3. ページ上部の「早押し機（Arduino）を使う場合はこちら」を開き、「接続する」を
   クリック。表示されるダイアログで早押し機のポートを選択する
4. これで準備完了です。ボタンが押されると自動で認識されます

参考として、このキットのファームウェア（工場出荷時のものと同じ内容）を
`arduino/rp2040_10button_kit/hayaoshi_serial.ino` に同梱しています。書き込み直す
必要は基本的にありませんが、万が一ファームウェアを入れ直す場合の参考にしてください。

### 6-B. 自分でArduinoを組み立てる場合（キットが無い場合）

#### 6-B-1. 用意するもの

- Arduino本体（Arduino Uno など、デジタル入力ピンが使えるもの）
- 押しボタンスイッチ（人数分、最大8個まで初期設定でそのまま使えます）
- ジャンパー線
- Arduino IDE（プログラム書き込み用ソフト。 https://www.arduino.cc/en/software
  から無料でダウンロードできます）
- ArduinoとPCをつなぐUSBケーブル

#### 6-B-2. 配線

1. 各ボタンの片方の足をArduinoのデジタルピン（2番、3番、4番…と順番に、
   最大9番まで）へ接続
2. 各ボタンのもう片方の足をArduinoの `GND` ピンへ接続
   （すべてのボタンのGND側を1本のGNDピンにまとめて繋いでも構いません）
3. 抵抗は不要です（プログラム側で内部プルアップ抵抗を使う設定にしています）

#### 6-B-3. スケッチの書き込み

1. Arduino IDEを起動する
2. 「ファイル」→「開く」で、プロジェクト内の
   `arduino/buzzer_sketch/buzzer_sketch.ino` を選んで開く
3. 「ツール」メニューで、使用しているArduinoの機種（例: Arduino Uno）と、
   接続しているシリアルポート（例: `COM3` や `/dev/cu.usbmodemXXXX`）を選択
4. 左上の「→（マイコンボードに書き込む）」ボタンをクリック
5. 「書き込みが完了しました」のようなメッセージが出れば成功です

#### 6-B-4. 動作テスト（任意）

Arduino IDEの「ツール」→「シリアルモニタ」を開き、ボーレートを `9600` に
設定した状態でボタンを押すと、`BUZZ,1` のような文字列が表示されれば
正しく動作しています。

---

## 7. 当日の運営フロー（チェックリスト）

- [ ] 開始15分前: 公開URLをブラウザで開き、サーバーを起こしておく
- [ ] 判定者: URLを開き「部屋を作る」→ 部屋名・パスワードを決めて作成
- [ ] 判定者: 同じ部屋名で「判定者として入る」→ 判定者画面を開いたままにする
- [ ] 回答者: 各自のタブレットでURLを開き、「回答者として入る」で入室
      （早押し機を使う人だけボタン番号も入力。使わない人は空欄でOK）
- [ ] 投影PC: URLの「プロジェクター表示画面」から部屋名を入力して開く
- [ ] （使う場合）投影PCで、投影画面の「早押し機（Arduino）を使う場合はこちら」を
      開き、「接続する」でArduinoを選択
- [ ] 判定者: 必要なら「得点ルール設定」で点数を大会ルールに合わせて調整
      （3着以降のルールを使う場合は先に追加しておく）
- [ ] 判定者が「次の問題へ」を押して進行開始

困ったときは `README.md` の「7. 制限事項・注意点」も合わせて確認してください。
